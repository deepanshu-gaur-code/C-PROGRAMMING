#include <stdio.h>

int main(){
    int i = 0;
    do{
        printf("The value of i is %d\n", i);
        i++;
    } while(i < 4); // The loop will run as long as i is less than 4
    return 0;
}